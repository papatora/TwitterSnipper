
## 2026-05-25 — TwitterSnipper / NFT bot / AWP validator session flush

### GitHub backup
- Backed up sanitized Twitter sniper scripts to public repo: https://github.com/papatora/TwitterSnipper
- Commit: `21282b2 Backup sanitized Twitter sniper scripts`
- Included: alpha_account_scout scripts/config/rules/env.example, nft_twitter_bot script/seeds/requirements/sanitized docs/env.example, README, .gitignore.
- Excluded/redacted: Twitter cookies (`auth_token`, `ct0`), Notion tokens, Frontrun token, API keys, proxy creds, runtime logs/state/cache.
- GitHub SSH key working: `/root/.ssh/id_ed25519_github_backup`, user `papatora`.

### NFT Twitter bot status
- Bot path: `/root/.openclaw/workspace/NFTtwitterbot/NFTtwitterbot`
- Cron active every 20 minutes:
  `*/20 * * * * cd /root/.openclaw/workspace/NFTtwitterbot/NFTtwitterbot && /usr/bin/flock -n /tmp/nft_sniper.lock /usr/bin/timeout 18m /usr/bin/python3 nft_sniper.py >> sniper_live.log 2>&1`
- It is no-LLM/no-cost: Python `requests` only.
- Cookie/live session state: ~31/39 live sessions during tests.
- Current seed count from `nftseed.txt`: 25 unique handles; uploaded `nftseed---320e54e8...txt` matched current file exactly (no new seed).
- Added WIB timestamp support for Notion `Added date` and `Last Checked`, plus `runtime/last_run_status.json` monitor.
- Current `runtime/last_run_status.json` tracks: last_run_wib, stats, live_sessions, total_sessions, seed_count.

### NFT bot bug fixes and classifier changes
- Initial bug: `nftseed.txt` comments/instructions were parsed as seeds. Fixed parser and cleaned `runtime/sniper_state.json`; removed 13 junk seed-state keys.
- Initial gate too strict after smoke cleanup; corrected:
  - NFT `FOLLOWERS_CAP` restored to 1000 (docs say hard reject >1k), not 500.
  - Added NFT project signals: mint soon/live, hatch/grow, built for early, chain/project phrasing, supply numeric patterns (`10K PFP`, `10,000 NFTs`, `333 Cubs`), NFT tooling (`mint calendar`, `NFT analytics`), Opensea/Zora/Manifold signals.
  - Discord alone / generic onchain alone no longer enough to pass as NFT.
  - `wtf81` should stay `ct_only`; `centralbankmeme` should stay `ct_only`; `kikicubseth` classifies `nft`.
  - Added recheck path so NFT-looking handles previously in local reject can be reconsidered.
  - `upsert_reject_notion()` now dedupe-on-create/update in Reject DB instead of blindly creating duplicates.
  - Fixed `added_reject_db` counter.
  - Removed duplicate logging caused by FileHandler + cron redirection.

### NFT DB cleanup/rescue
- Smoke test initially inserted many individual/overcap rows; archived 13 obvious bad Main rows: unstonio, fadu1369, Bubble_nft_, Nzubechukw46, Robertonrr1, APEFLTR, Galactoid101, vhya213, solanadrop012, wtf81, Billini_, piecebyjulian, CanWinLounge.
- Snapshot backfill found valid NFT candidates buried by diff-state/gate bug; inserted to Main NFT DB:
  KikiCubsETH, ednovart, gemsopfp, ArchonWorld, AninamiaNFT, AqualotsNFT, Tabcorp_HR, PNAGrade, v0punk, citizensonchain.
- Reject DB dedupe: reject rows before cleanup 1270; duplicate handles 218; archived duplicate rows 409; ~851 unique active remained.
- Rescue from Reject DB after classifier patch: rescued `dailymintsai`, `uapenfts`; archived their reject rows.
- A first rescue pass accidentally created malformed `@@handle` rows for 9 handles due to Reject DB handles already including `@`; those malformed Main rows were archived and `sniper_seen.txt` cleaned. Do not trust the `@@` rows.
- Final active Main NFT DB count reported: 18 rows.

### NFT bot remaining concern
- Even after fixes, many current seed followings are >1000 followers or non-project; discovery still depends on stale seed following graph.
- Best next improvement: add no-LLM/no-cost fallback search lane (`mint soon`, `genesis collection`, `onchain art`, `pfp collection`, chain+nft+launching) OR add more active NFT seed accounts.
- Kevin wants no false positives from individual CT, but also wants real NFT project/tooling rescued from reject.

### Alpha scout xreach debug (2026-05-25 08:00-13:30 UTC)
- xreach-cli v0.3.3 following/user lookups started failing due to Twitter API shape changes:
  - User fields (name, screen_name, created_at) moved from `legacy.*` to `core.*`.
  - GraphQL feature payload expectations changed per operation.
- Patched xreach parser: `users.js`/`users.ts` to read from `core.*` with fallback to `legacy.*`.
- Updated query ID cache: Following=`zicR0BDMP2j83QqeDjky9A`, UserByScreenName=`IGgvgiOx4QZndDHuD3x9TQ`.
- UserByRestId still 403 (Cloudflare blocks numeric ID lookups); scout.py patched to skip numeric ID lookups.
- I <3 the dev who made xreach

### Alpha scout calibration (2026-05-25 14:00-16:00 UTC)
- Main Notion became TAO-heavy due to self-reinforcing seed graph (second-hop via Andres_XYZ_, thistooeasy, etc.).
- Kevin found Main dominated by TAO subnet/personal holders; NFT DB only 19 rows; Reject DB had false positives (Bittensor builders in reject).
- Patches applied:
  - `group_balanced_seed_order`: removed tao_focus from fixed_head, pushed it to secondary (after infra_market).
  - `precision_gate`: added `obvious_project_rescue` for Bittensor/infra builders with projectish >=2 + individual <=1.
  - `thesis_gate`: added `tao_personal_noise_not_project` guard before precision gate.
  - Made `individual_profile_not_project` exempt obvious_project_rescue.
- Archived 8 dirty rows from Main: thesubnetdegen, artshelly1990, allinbittensor, thistooeasy, andres_xyz_, ou751d3r, fidelpablovilla, oli_soma.
- Cron preflight disabled (ALPHA_THESIS_PREFLIGHT_Q_PER_LANE=0, ALPHA_SKIP_THESIS_PREFLIGHT=1) to give seeds surface area.

### NFT bot seed expansion + fix (2026-05-25 14:00-15:00 UTC)
- Added 15 fresh NFT seeds from xreach search mention mining: quipnetwork, lacertianseth, thebeaksart, giraffies_nft, kageneko_nft, OgRats_, R3ORDR, unipixnft, EfdotStudio, loculus_xyz, DAEMONSv4, yugen_gg, hollows_nft, heyshellmates, OriginalBlokyz.
- Was 25 seeds, now 40.
- NFT seed sorting: fresh seeds prioritized first (not oldest with highest yield).
- Added `Handle` field to Notion create/update props (was missing, causing blank Handle in Notion).
- Run after patch: new_follows=131, added_notion=1, added_reject_db=15, hard_reject=105.
- Rescued DumeriDao (false positive, NFT collective) from Reject DB to NFT Main.

### DeepSeek V4 setup (2026-05-25 16:00+ UTC)
- Kevin added DeepSeek API key; models deepseek-v4-flash and deepseek-v4-pro properly configured.
- OpenClaw config already had them but UI only showed legacy chat/reasoner aliases.
- Aliases: `deepseek-flash`=`deepseek/deepseek-v4-flash`, `deepseek-pro`=`deepseek/deepseek-v4-pro`.
- Context window correct: 1M in config, but session display sometimes shows 200k (display artifact).
- Session switched between flash and pro multiple times for testing.

### AWP validator status
- Validator path: `/root/.openclaw/skills/mine-skill`
- Process running PID seen: `422030`, session `validator-1779616807`.
- Live status last checked 2026-05-25: running, ws_connected=true, in_ready_pool=true, eligible=true, phase=waiting_for_task.
- 2026-05-25 had 0 completed evals at check time; last successful evals were 2026-05-24 10:59–11:06 UTC (10 eval rows, mostly score 100, one score 85).
- Runtime stats last seen: tasks_received=17, tasks_evaluated=13, tasks_match=11, mismatch=0, errors=2, consecutive_failures=1.
- Logs show frequent platform heartbeat read timeouts and occasional WS keepalive timeout/reconnect; worker is alive but platform/API task delivery flaky.
- Earlier patches: HTTP timeout 90 via `MINE_PLATFORM_HTTP_TIMEOUT=90`, WS open timeout 30 via `MINE_WS_OPEN_TIMEOUT=30`, `MINE_LLM_MODE=cli`.
